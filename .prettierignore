.next/
node_modules/
out/
build/
screenshots/
package-lock.json
next-env.d.ts
*.woff2

# Governance-Dokument: wortgleich so gepflegt, wie es abgestimmt wurde.
CLAUDE.md

# Wird von `next build` automatisch neu geschrieben – Prettier und Next
# wuerden sich sonst gegenseitig ueberschreiben.
tsconfig.json

# Wahrheitsquelle fuer Produktaussagen: wortgleich so gepflegt, wie sie
# abgestimmt wurde. Prettier wuerde Tabellen und Zeilenumbrueche umformatieren.
docs/produktstand-2026-08.md

# Zielbild-Dokument aus der Geschaeftsfuehrung: wortgleich so abgelegt, wie es
# uebergeben wurde. Prettier wuerde Absaetze umbrechen und damit den Eindruck
# erwecken, jemand haette den Text redigiert.
docs/selyvi-kompakt.md

# Kommentierte Vorlage – Prettier kennt kein dotenv-Format.
.env.example

# Referenz-Dokumentation: Tabellen sind auf Lesbarkeit gesetzt, nicht auf
# Prettier-Spaltenbreite.
docs/app-referenz/ABWEICHUNGEN.md

# ERZEUGTE MESSBERICHTE – nicht von Hand anfassen.
#
# Beide entstehen aus `npm run audit:en` bzw. `npm run redundanz:en`. Prettier
# wuerde die Spaltenbreiten der Tabellen umbrechen, und beim naechsten Lauf
# schriebe das Skript sie zurueck – ein Hin und Her im Diff, das niemandem
# hilft. Wer die Darstellung aendern will, aendert das Skript und laesst
# `npm run audit:en -- --nur-bericht` laufen.
docs/mess-basis-en.md
docs/mess-basis-en.json
docs/redundanz-en.md
